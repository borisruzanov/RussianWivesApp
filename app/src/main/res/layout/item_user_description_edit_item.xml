<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="@dimen/dp12"
    android:paddingLeft="@dimen/dp12"
    android:paddingRight="@dimen/dp12">

    <LinearLayout
        android:id="@+id/item_user_description_edit_linear"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/item_user_edit_profile_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:padding="@dimen/dp8"
            android:text="@string/title_text"
            android:textColor="@android:color/black"
            android:textSize="@dimen/answers_text_size" />

        <ImageView
            android:id="@+id/profile_settings_tv_image_arrow"
            android:layout_gravity="center_vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="4"
            android:textColor="@android:color/black"
            android:textSize="@dimen/answers_text_size"
            android:contentDescription="@string/profile_arrow_desc"
            app:srcCompat="@drawable/ic_keyboard_arrow_right_black_24dp" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/user_desc_view_height"
        android:background="@color/grey_600" />
</RelativeLayout>